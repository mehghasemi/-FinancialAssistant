# FinancialAssistant

- ساختار: `app/` بک‌اند، `static/` رابط کاربری، `tests/` تست‌ها، `scripts/` ابزارهای ورود داده، `release/` فایل اجرایی.
- معماری: وب‌اپ محلی FastAPI با SQLite؛ رابط فارسی RTL و تاریخ‌های شمسی.
- فناوری‌ها: Python، FastAPI، Uvicorn، SQLite، jdatetime، JavaScript و CSS بدون وابستگی CDN.
- اجرا در توسعه: `./run.ps1`؛ اجرای نهایی: `release/FinancialAssistant.exe`.
- فایل‌های کلیدی: `app/main.py`، `app/database.py`، `static/index.html`، `static/app.js`، `static/styles.css`.
- دادهٔ کاربر در دیتابیس محلی است؛ هرگز آن یا فایل‌های بکاپ را وارد Git نکن.
- قبل از تغییر، علت و وابستگی‌های بخش مربوط را بررسی کن؛ تغییرات کوچک و هم‌راستا باشند.
- بازنویسی کامل فایل‌های موجود ممنوع است مگر ضرورت روشن داشته باشد.
- پس از تغییر، تست‌های مرتبط و بررسی نحوی را اجرا کن؛ از Regression جلوگیری کن.
- در پاسخ فقط نتیجه و بخش‌های تغییرکرده را کوتاه گزارش کن.
- عبارت «پایان فاز قبلی» یعنی درخواست بعدی مستقل از زمینهٔ قبلی است.
